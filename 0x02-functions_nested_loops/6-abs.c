#include "main.h"
/**
 *_abs - computes the absolute value of an integer
 *@absVal: the intiger to be computed
 * Return: the absolute value
 */
int _abs(int absVal);
{
	if (absVal >= 0)
		return (absVal);
	else
		return (-absVal);
}
